<view wx:if="{{id&&isLoad===null}}" class="margin-top load notexist text-l text-grey"></view>
<view wx:if="{{id&&isLoad===false}}" class="margin-top load loading text-l text-grey"></view>
<block wx:if="{{isAdmin&& isLoad}}">
	<view class="main-admin">

		<include src="../add/admin_member_form_tpl.wxml" />
 
		<button bindtap="bindFormEditSubmit" class="btn-admin margin-top-l">保存</button>

		<view class="margin-top-l">
			<button bindtap="bindUnbindWxTap" class="btn-admin btn-line-red">解绑该人员微信号</button>
		</view>

	</view>


</block>